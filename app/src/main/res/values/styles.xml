<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!-- <item name="android:windowTitleBackgroundStyle">@style/WindowTitleBackground</item> -->
        <item name="android:actionBarStyle">@style/WindowTitleBackground</item>
    </style>

    <style name="WindowTitleBackground" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">#47A414</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="AppFrameTheme">
        <item name="android:background">#F0F0E0</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="AppContentTheme">
        <item name="android:background">#FFFFFF</item>
        <item name="android:textColor">#000000</item>
    </style>

</resources>
